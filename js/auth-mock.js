/* ============================================================
   AUTH-MOCK.JS — DEPRECATED
   Este arquivo foi o mock de autenticação via localStorage.
   Foi substituído por js/api.js, que faz chamadas reais ao
   backend Spring Boot.

   NÃO USE ESTE ARQUIVO. Ele existe apenas como referência
   histórica. Todos os scripts agora dependem de api.js.
   ============================================================ */
